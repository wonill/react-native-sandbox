import {StyleSheet, View} from 'react-native';

interface AuthHomeScrrenProps {}

export default function AuthHomeScreen({}: AuthHomeScrrenProps) {
  return <View></View>;
}

const styles = StyleSheet.create({});
